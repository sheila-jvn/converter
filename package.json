{
  "name": "converter",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build:linux": "bun build ./src/index.ts --minify --compile --sourcemap --external=sharp --outfile=./dist/converter --target=bun-linux-x64"
  },
  "type": "module"
}
